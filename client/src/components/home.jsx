import '../App.css'

const Home = () => {
  return (
    <div className='home'>
      <h1>Welcome to the Home page.</h1>
      <div>
        <span>
         <h4>Want to see Dashboard ? </h4>
          </span>
        <button>Go to Dashboard</button>
      </div>
    </div>
  );
};

export default Home;
